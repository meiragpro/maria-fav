import { defineConfig } from 'astro/config';

// Update `site` to your final URL once you know it
// (e.g. https://marias-fav.netlify.app or your custom domain).
export default defineConfig({
  site: 'https://marias-fav.netlify.app',
});
